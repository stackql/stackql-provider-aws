openapi: 3.1.0
info:
  contact:
    name: StackQL Studios
    url: https://stackql.io/
    email: info@stackql.io
  x-stackql-serviceName: route53_recovery_readiness
  x-aws-serviceName: route53recoveryreadiness
  x-aws-protocol: aws.protocols#restJson1
  x-dateGenerated: '2025-05-07'
  version: '2019-12-02'
  title: AWS Route53 Recovery Readiness
  description: |-
    Recovery readiness
servers:
- description: The AWS Route53 Recovery Readiness regional endpoint
  url: https://route53-recovery-readiness.{region}.amazonaws.com
  variables:
    region:
      description: The AWS region
      enum:
      - us-east-1
      - us-east-2
      - us-west-1
      - us-west-2
      - us-gov-west-1
      - us-gov-east-1
      - ca-central-1
      - eu-north-1
      - eu-west-1
      - eu-west-2
      - eu-west-3
      - eu-central-1
      - eu-south-1
      - af-south-1
      - ap-northeast-1
      - ap-northeast-2
      - ap-northeast-3
      - ap-southeast-1
      - ap-southeast-2
      - ap-east-1
      - ap-south-1
      - sa-east-1
      - me-south-1
      default: us-east-1
paths:
  /cells:
    post:
      operationId: CreateCell
      description: |-
        Creates a cell in an account.
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                CellName:
                  $ref: '#/components/schemas/__string'
                Cells:
                  $ref: '#/components/schemas/__listOf__string'
                Tags:
                  $ref: '#/components/schemas/Tags'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '409':
          description: |-
            Updating or deleting a resource can cause an inconsistent state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
    get:
      operationId: ListCells
      description: |-
        Lists the cells for an account.
      parameters:
      - name: maxResults
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/MaxResults'
      - name: nextToken
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
  /crossaccountauthorizations:
    post:
      operationId: CreateCrossAccountAuthorization
      description: |-
        Creates a cross-account readiness authorization. This lets you authorize another account to work with Route 53 Application Recovery Controller, for example, to check the readiness status of resources in a separate account.
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                CrossAccountAuthorization:
                  $ref: '#/components/schemas/CrossAccountAuthorization'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '409':
          description: |-
            Updating or deleting a resource can cause an inconsistent state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
    get:
      operationId: ListCrossAccountAuthorizations
      description: |-
        Lists the cross-account readiness authorizations that are in place for an account.
      parameters:
      - name: maxResults
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/MaxResults'
      - name: nextToken
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
  /readinesschecks:
    post:
      operationId: CreateReadinessCheck
      description: |-
        Creates a readiness check in an account. A readiness check monitors a resource set in your application, such as a set of Amazon Aurora instances, that Application Recovery Controller is auditing recovery readiness for. The audits run once every minute on every resource that's associated with a readiness check.
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                ReadinessCheckName:
                  $ref: '#/components/schemas/__string'
                ResourceSetName:
                  $ref: '#/components/schemas/__string'
                Tags:
                  $ref: '#/components/schemas/Tags'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '409':
          description: |-
            Updating or deleting a resource can cause an inconsistent state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
    get:
      operationId: ListReadinessChecks
      description: |-
        Lists the readiness checks for an account.
      parameters:
      - name: maxResults
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/MaxResults'
      - name: nextToken
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
  /recoverygroups:
    post:
      operationId: CreateRecoveryGroup
      description: |-
        Creates a recovery group in an account. A recovery group corresponds to an application and includes a list of the cells that make up the application.
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                Cells:
                  $ref: '#/components/schemas/__listOf__string'
                RecoveryGroupName:
                  $ref: '#/components/schemas/__string'
                Tags:
                  $ref: '#/components/schemas/Tags'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '409':
          description: |-
            Updating or deleting a resource can cause an inconsistent state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
    get:
      operationId: ListRecoveryGroups
      description: |-
        Lists the recovery groups in an account.
      parameters:
      - name: maxResults
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/MaxResults'
      - name: nextToken
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
  /resourcesets:
    post:
      operationId: CreateResourceSet
      description: |-
        Creates a resource set. A resource set is a set of resources of one type that span multiple cells. You can associate a resource set with a readiness check to monitor the resources for failover readiness.
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                ResourceSetName:
                  $ref: '#/components/schemas/__string'
                ResourceSetType:
                  $ref: '#/components/schemas/__stringPatternAWSAZaZ09AZaZ09'
                Resources:
                  $ref: '#/components/schemas/__listOfResource'
                Tags:
                  $ref: '#/components/schemas/Tags'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '409':
          description: |-
            Updating or deleting a resource can cause an inconsistent state.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConflictException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
    get:
      operationId: ListResourceSets
      description: |-
        Lists the resource sets in an account.
      parameters:
      - name: maxResults
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/MaxResults'
      - name: nextToken
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
  /cells/{CellName}:
    delete:
      operationId: DeleteCell
      description: |-
        Delete a cell. When successful, the response code is 204, with no response body.
      parameters:
      - name: CellName
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '204': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '404':
          description: |-
            The requested resource does not exist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceNotFoundException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
    get:
      operationId: GetCell
      description: |-
        Gets information about a cell including cell name, cell Amazon Resource Name (ARN), ARNs of nested cells for this cell, and a list of those cell ARNs with their associated recovery group ARNs.
      parameters:
      - name: CellName
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '404':
          description: |-
            The requested resource does not exist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceNotFoundException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
    put:
      operationId: UpdateCell
      description: |-
        Updates a cell to replace the list of nested cells with a new list of nested cells.
      parameters:
      - name: CellName
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                Cells:
                  $ref: '#/components/schemas/__listOf__string'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '404':
          description: |-
            The requested resource does not exist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceNotFoundException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
  /crossaccountauthorizations/{CrossAccountAuthorization}:
    delete:
      operationId: DeleteCrossAccountAuthorization
      description: |-
        Deletes cross account readiness authorization.
      parameters:
      - name: CrossAccountAuthorization
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
  /readinesschecks/{ReadinessCheckName}:
    delete:
      operationId: DeleteReadinessCheck
      description: |-
        Deletes a readiness check.
      parameters:
      - name: ReadinessCheckName
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '204': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '404':
          description: |-
            The requested resource does not exist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceNotFoundException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
    get:
      operationId: GetReadinessCheck
      description: |-
        Gets details about a readiness check.
      parameters:
      - name: ReadinessCheckName
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '404':
          description: |-
            The requested resource does not exist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceNotFoundException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
    put:
      operationId: UpdateReadinessCheck
      description: |-
        Updates a readiness check.
      parameters:
      - name: ReadinessCheckName
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                ResourceSetName:
                  $ref: '#/components/schemas/__string'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '404':
          description: |-
            The requested resource does not exist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceNotFoundException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
  /recoverygroups/{RecoveryGroupName}:
    delete:
      operationId: DeleteRecoveryGroup
      description: |-
        Deletes a recovery group.
      parameters:
      - name: RecoveryGroupName
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '204': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '404':
          description: |-
            The requested resource does not exist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceNotFoundException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
    get:
      operationId: GetRecoveryGroup
      description: |-
        Gets details about a recovery group, including a list of the cells that are included in it.
      parameters:
      - name: RecoveryGroupName
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '404':
          description: |-
            The requested resource does not exist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceNotFoundException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
    put:
      operationId: UpdateRecoveryGroup
      description: |-
        Updates a recovery group.
      parameters:
      - name: RecoveryGroupName
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                Cells:
                  $ref: '#/components/schemas/__listOf__string'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '404':
          description: |-
            The requested resource does not exist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceNotFoundException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
  /resourcesets/{ResourceSetName}:
    delete:
      operationId: DeleteResourceSet
      description: |-
        Deletes a resource set.
      parameters:
      - name: ResourceSetName
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '204': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '404':
          description: |-
            The requested resource does not exist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceNotFoundException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
    get:
      operationId: GetResourceSet
      description: |-
        Displays the details about a resource set, including a list of the resources in the set.
      parameters:
      - name: ResourceSetName
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '404':
          description: |-
            The requested resource does not exist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceNotFoundException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
    put:
      operationId: UpdateResourceSet
      description: |-
        Updates a resource set.
      parameters:
      - name: ResourceSetName
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                ResourceSetType:
                  $ref: '#/components/schemas/__stringPatternAWSAZaZ09AZaZ09'
                Resources:
                  $ref: '#/components/schemas/__listOfResource'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '404':
          description: |-
            The requested resource does not exist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceNotFoundException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
  /recoverygroups/{RecoveryGroupName}/architectureRecommendations:
    get:
      operationId: GetArchitectureRecommendations
      description: |-
        Gets recommendations about architecture designs for improving resiliency for an application, based on a recovery group.
      parameters:
      - name: maxResults
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/MaxResults'
      - name: nextToken
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/__string'
      - name: RecoveryGroupName
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '404':
          description: |-
            The requested resource does not exist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceNotFoundException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
  /cellreadiness/{CellName}:
    get:
      operationId: GetCellReadinessSummary
      description: |-
        Gets readiness for a cell. Aggregates the readiness of all the resources that are associated with the cell into a single value.
      parameters:
      - name: CellName
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      - name: maxResults
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/MaxResults'
      - name: nextToken
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '404':
          description: |-
            The requested resource does not exist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceNotFoundException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
  /readinesschecks/{ReadinessCheckName}/resource/{ResourceIdentifier}/status:
    get:
      operationId: GetReadinessCheckResourceStatus
      description: |-
        Gets individual readiness status for a readiness check. To see the overall readiness status for a recovery group, that considers the readiness status for all the readiness checks in the recovery group, use GetRecoveryGroupReadinessSummary.
      parameters:
      - name: maxResults
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/MaxResults'
      - name: nextToken
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/__string'
      - name: ReadinessCheckName
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      - name: ResourceIdentifier
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '404':
          description: |-
            The requested resource does not exist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceNotFoundException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
  /readinesschecks/{ReadinessCheckName}/status:
    get:
      operationId: GetReadinessCheckStatus
      description: |-
        Gets the readiness status for an individual readiness check. To see the overall readiness status for a recovery group, that considers the readiness status for all the readiness checks in a recovery group, use GetRecoveryGroupReadinessSummary.
      parameters:
      - name: maxResults
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/MaxResults'
      - name: nextToken
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/__string'
      - name: ReadinessCheckName
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '404':
          description: |-
            The requested resource does not exist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceNotFoundException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
  /recoverygroupreadiness/{RecoveryGroupName}:
    get:
      operationId: GetRecoveryGroupReadinessSummary
      description: |-
        Displays a summary of information about a recovery group's readiness status. Includes the readiness checks for resources in the recovery group and the readiness status of each one.
      parameters:
      - name: maxResults
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/MaxResults'
      - name: nextToken
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/__string'
      - name: RecoveryGroupName
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '404':
          description: |-
            The requested resource does not exist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceNotFoundException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
  /rules:
    get:
      operationId: ListRules
      description: |-
        Lists all readiness rules, or lists the readiness rules for a specific resource type.
      parameters:
      - name: maxResults
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/MaxResults'
      - name: nextToken
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/__string'
      - name: resourceType
        in: query
        required: false
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '200': {}
        '403':
          description: |-
            User does not have sufficient access to perform this action.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccessDeniedException'
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '429':
          description: |-
            Request was denied due to request throttling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThrottlingException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
  /tags/{ResourceArn}:
    get:
      operationId: ListTagsForResources
      description: |-
        Lists the tags for a resource.
      parameters:
      - name: ResourceArn
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      responses:
        '200': {}
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '404':
          description: |-
            The requested resource does not exist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceNotFoundException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
    post:
      operationId: TagResource
      description: |-
        Adds a tag to a resource.
      parameters:
      - name: ResourceArn
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                Tags:
                  $ref: '#/components/schemas/Tags'
      responses:
        '200': {}
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '404':
          description: |-
            The requested resource does not exist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceNotFoundException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
    delete:
      operationId: UntagResource
      description: |-
        Removes a tag from a resource.
      parameters:
      - name: ResourceArn
        in: path
        required: true
        schema:
          $ref: '#/components/schemas/__string'
      - name: tagKeys
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/__listOf__string'
      responses:
        '204': {}
        '500':
          description: |-
            An unexpected error occurred.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InternalServerException'
        '404':
          description: |-
            The requested resource does not exist.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceNotFoundException'
        '400':
          description: |-
            The input fails to satisfy the constraints specified by an AWS service.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationException'
components:
  schemas:
    AccessDeniedException:
      type: object
      properties:
        Message:
          $ref: '#/components/schemas/__string'
      description: User does not have sufficient access to perform this action.
    CellOutput:
      type: object
      properties:
        CellArn:
          allOf:
          - $ref: '#/components/schemas/__stringMax256'
          description: The Amazon Resource Name (ARN) for the cell.
        CellName:
          allOf:
          - $ref: '#/components/schemas/__stringMax64PatternAAZAZ09Z'
          description: The name of the cell.
        Cells:
          allOf:
          - $ref: '#/components/schemas/__listOf__string'
          description: A list of cell ARNs.
        ParentReadinessScopes:
          allOf:
          - $ref: '#/components/schemas/__listOf__string'
          description: The readiness scope for the cell, which can be a cell Amazon
            Resource Name (ARN) or a recovery group ARN. This is a list but currently
            can have only one element.
        Tags:
          allOf:
          - $ref: '#/components/schemas/Tags'
          description: Tags on the resources.
      required:
      - CellArn
      - CellName
      - Cells
      - ParentReadinessScopes
      description: Information about a cell.
    ConflictException:
      type: object
      properties:
        Message:
          $ref: '#/components/schemas/__string'
      description: Updating or deleting a resource can cause an inconsistent state.
    CreateCellRequest:
      type: object
      properties:
        CellName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The name of the cell to create.
        Cells:
          allOf:
          - $ref: '#/components/schemas/__listOf__string'
          description: A list of cell Amazon Resource Names (ARNs) contained within
            this cell, for use in nested cells. For example, Availability Zones within
            specific Amazon Web Services Regions.
        Tags:
          $ref: '#/components/schemas/Tags'
      required:
      - CellName
    CreateCellResponse:
      type: object
      properties:
        CellArn:
          allOf:
          - $ref: '#/components/schemas/__stringMax256'
          description: The Amazon Resource Name (ARN) for the cell.
        CellName:
          allOf:
          - $ref: '#/components/schemas/__stringMax64PatternAAZAZ09Z'
          description: The name of the cell.
        Cells:
          allOf:
          - $ref: '#/components/schemas/__listOf__string'
          description: A list of cell ARNs.
        ParentReadinessScopes:
          allOf:
          - $ref: '#/components/schemas/__listOf__string'
          description: The readiness scope for the cell, which can be a cell Amazon
            Resource Name (ARN) or a recovery group ARN. This is a list but currently
            can have only one element.
        Tags:
          allOf:
          - $ref: '#/components/schemas/Tags'
          description: Tags on the resources.
    CreateCrossAccountAuthorizationRequest:
      type: object
      properties:
        CrossAccountAuthorization:
          allOf:
          - $ref: '#/components/schemas/CrossAccountAuthorization'
          description: The cross-account authorization.
      required:
      - CrossAccountAuthorization
    CreateCrossAccountAuthorizationResponse:
      type: object
      properties:
        CrossAccountAuthorization:
          allOf:
          - $ref: '#/components/schemas/CrossAccountAuthorization'
          description: The cross-account authorization.
    CreateReadinessCheckRequest:
      type: object
      properties:
        ReadinessCheckName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The name of the readiness check to create.
        ResourceSetName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The name of the resource set to check.
        Tags:
          $ref: '#/components/schemas/Tags'
      required:
      - ReadinessCheckName
      - ResourceSetName
    CreateReadinessCheckResponse:
      type: object
      properties:
        ReadinessCheckArn:
          allOf:
          - $ref: '#/components/schemas/__stringMax256'
          description: The Amazon Resource Name (ARN) associated with a readiness
            check.
        ReadinessCheckName:
          allOf:
          - $ref: '#/components/schemas/__stringMax64PatternAAZAZ09Z'
          description: Name of a readiness check.
        ResourceSet:
          allOf:
          - $ref: '#/components/schemas/__stringMax64PatternAAZAZ09Z'
          description: Name of the resource set to be checked.
        Tags:
          $ref: '#/components/schemas/Tags'
    CreateRecoveryGroupRequest:
      type: object
      properties:
        Cells:
          allOf:
          - $ref: '#/components/schemas/__listOf__string'
          description: A list of the cell Amazon Resource Names (ARNs) in the recovery
            group.
        RecoveryGroupName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The name of the recovery group to create.
        Tags:
          $ref: '#/components/schemas/Tags'
      required:
      - RecoveryGroupName
    CreateRecoveryGroupResponse:
      type: object
      properties:
        Cells:
          allOf:
          - $ref: '#/components/schemas/__listOf__string'
          description: A list of a cell's Amazon Resource Names (ARNs).
        RecoveryGroupArn:
          allOf:
          - $ref: '#/components/schemas/__stringMax256'
          description: The Amazon Resource Name (ARN) for the recovery group.
        RecoveryGroupName:
          allOf:
          - $ref: '#/components/schemas/__stringMax64PatternAAZAZ09Z'
          description: The name of the recovery group.
        Tags:
          allOf:
          - $ref: '#/components/schemas/Tags'
          description: The tags associated with the recovery group.
    CreateResourceSetRequest:
      type: object
      properties:
        ResourceSetName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The name of the resource set to create.
        ResourceSetType:
          allOf:
          - $ref: '#/components/schemas/__stringPatternAWSAZaZ09AZaZ09'
          description: 'The resource type of the resources in the resource set. Enter
            one of the following values for resource type:


            AWS::ApiGateway::Stage, AWS::ApiGatewayV2::Stage, AWS::AutoScaling::AutoScalingGroup,
            AWS::CloudWatch::Alarm, AWS::EC2::CustomerGateway, AWS::DynamoDB::Table,
            AWS::EC2::Volume, AWS::ElasticLoadBalancing::LoadBalancer, AWS::ElasticLoadBalancingV2::LoadBalancer,
            AWS::Lambda::Function, AWS::MSK::Cluster, AWS::RDS::DBCluster, AWS::Route53::HealthCheck,
            AWS::SQS::Queue, AWS::SNS::Topic, AWS::SNS::Subscription, AWS::EC2::VPC,
            AWS::EC2::VPNConnection, AWS::EC2::VPNGateway, AWS::Route53RecoveryReadiness::DNSTargetResource'
        Resources:
          allOf:
          - $ref: '#/components/schemas/__listOfResource'
          description: A list of resource objects in the resource set.
        Tags:
          allOf:
          - $ref: '#/components/schemas/Tags'
          description: A tag to associate with the parameters for a resource set.
      required:
      - ResourceSetName
      - ResourceSetType
      - Resources
    CreateResourceSetResponse:
      type: object
      properties:
        ResourceSetArn:
          allOf:
          - $ref: '#/components/schemas/__stringMax256'
          description: The Amazon Resource Name (ARN) for the resource set.
        ResourceSetName:
          allOf:
          - $ref: '#/components/schemas/__stringMax64PatternAAZAZ09Z'
          description: The name of the resource set.
        ResourceSetType:
          allOf:
          - $ref: '#/components/schemas/__stringPatternAWSAZaZ09AZaZ09'
          description: 'The resource type of the resources in the resource set. Enter
            one of the following values for resource type:


            AWS::ApiGateway::Stage, AWS::ApiGatewayV2::Stage, AWS::AutoScaling::AutoScalingGroup,
            AWS::CloudWatch::Alarm, AWS::EC2::CustomerGateway, AWS::DynamoDB::Table,
            AWS::EC2::Volume, AWS::ElasticLoadBalancing::LoadBalancer, AWS::ElasticLoadBalancingV2::LoadBalancer,
            AWS::Lambda::Function, AWS::MSK::Cluster, AWS::RDS::DBCluster, AWS::Route53::HealthCheck,
            AWS::SQS::Queue, AWS::SNS::Topic, AWS::SNS::Subscription, AWS::EC2::VPC,
            AWS::EC2::VPNConnection, AWS::EC2::VPNGateway, AWS::Route53RecoveryReadiness::DNSTargetResource'
        Resources:
          allOf:
          - $ref: '#/components/schemas/__listOfResource'
          description: A list of resource objects.
        Tags:
          $ref: '#/components/schemas/Tags'
    CrossAccountAuthorization:
      type: string
    DNSTargetResource:
      type: object
      properties:
        DomainName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The domain name that acts as an ingress point to a portion
            of the customer application.
        HostedZoneArn:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The hosted zone Amazon Resource Name (ARN) that contains the
            DNS record with the provided name of the target resource.
        RecordSetId:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The Route 53 record set ID that uniquely identifies a DNS record,
            given a name and a type.
        RecordType:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The type of DNS record of the target resource.
        TargetResource:
          allOf:
          - $ref: '#/components/schemas/TargetResource'
          description: The target resource of the DNS target resource.
      description: A component for DNS/routing control readiness checks and architecture
        checks.
    DeleteCellRequest:
      type: object
      properties:
        CellName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The name of the cell.
      required:
      - CellName
    DeleteCrossAccountAuthorizationRequest:
      type: object
      properties:
        CrossAccountAuthorization:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The cross-account authorization.
      required:
      - CrossAccountAuthorization
    DeleteCrossAccountAuthorizationResponse:
      type: object
      properties: {}
    DeleteReadinessCheckRequest:
      type: object
      properties:
        ReadinessCheckName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: Name of a readiness check.
      required:
      - ReadinessCheckName
    DeleteRecoveryGroupRequest:
      type: object
      properties:
        RecoveryGroupName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The name of a recovery group.
      required:
      - RecoveryGroupName
    DeleteResourceSetRequest:
      type: object
      properties:
        ResourceSetName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: Name of a resource set.
      required:
      - ResourceSetName
    GetArchitectureRecommendationsRequest:
      type: object
      properties:
        MaxResults:
          allOf:
          - $ref: '#/components/schemas/MaxResults'
          description: The number of objects that you want to return with this call.
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
        RecoveryGroupName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The name of a recovery group.
      required:
      - RecoveryGroupName
    GetArchitectureRecommendationsResponse:
      type: object
      properties:
        LastAuditTimestamp:
          allOf:
          - $ref: '#/components/schemas/LastAuditTimestamp'
          description: The time that a recovery group was last assessed for recommendations,
            in UTC ISO-8601 format.
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
        Recommendations:
          allOf:
          - $ref: '#/components/schemas/__listOfRecommendation'
          description: A list of the recommendations for the customer's application.
    GetCellReadinessSummaryRequest:
      type: object
      properties:
        CellName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The name of the cell.
        MaxResults:
          allOf:
          - $ref: '#/components/schemas/MaxResults'
          description: The number of objects that you want to return with this call.
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
      required:
      - CellName
    GetCellReadinessSummaryResponse:
      type: object
      properties:
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
        Readiness:
          allOf:
          - $ref: '#/components/schemas/Readiness'
          description: The readiness at a cell level.
        ReadinessChecks:
          allOf:
          - $ref: '#/components/schemas/__listOfReadinessCheckSummary'
          description: Summaries for the readiness checks that make up the cell.
    GetCellRequest:
      type: object
      properties:
        CellName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The name of the cell.
      required:
      - CellName
    GetCellResponse:
      type: object
      properties:
        CellArn:
          allOf:
          - $ref: '#/components/schemas/__stringMax256'
          description: The Amazon Resource Name (ARN) for the cell.
        CellName:
          allOf:
          - $ref: '#/components/schemas/__stringMax64PatternAAZAZ09Z'
          description: The name of the cell.
        Cells:
          allOf:
          - $ref: '#/components/schemas/__listOf__string'
          description: A list of cell ARNs.
        ParentReadinessScopes:
          allOf:
          - $ref: '#/components/schemas/__listOf__string'
          description: The readiness scope for the cell, which can be a cell Amazon
            Resource Name (ARN) or a recovery group ARN. This is a list but currently
            can have only one element.
        Tags:
          allOf:
          - $ref: '#/components/schemas/Tags'
          description: Tags on the resources.
    GetReadinessCheckRequest:
      type: object
      properties:
        ReadinessCheckName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: Name of a readiness check.
      required:
      - ReadinessCheckName
    GetReadinessCheckResourceStatusRequest:
      type: object
      properties:
        MaxResults:
          allOf:
          - $ref: '#/components/schemas/MaxResults'
          description: The number of objects that you want to return with this call.
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
        ReadinessCheckName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: Name of a readiness check.
        ResourceIdentifier:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The resource identifier, which is the Amazon Resource Name
            (ARN) or the identifier generated for the resource by Application Recovery
            Controller (for example, for a DNS target resource).
      required:
      - ReadinessCheckName
      - ResourceIdentifier
    GetReadinessCheckResourceStatusResponse:
      type: object
      properties:
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
        Readiness:
          allOf:
          - $ref: '#/components/schemas/Readiness'
          description: The readiness at a rule level.
        Rules:
          allOf:
          - $ref: '#/components/schemas/__listOfRuleResult'
          description: Details of the rule's results.
    GetReadinessCheckResponse:
      type: object
      properties:
        ReadinessCheckArn:
          allOf:
          - $ref: '#/components/schemas/__stringMax256'
          description: The Amazon Resource Name (ARN) associated with a readiness
            check.
        ReadinessCheckName:
          allOf:
          - $ref: '#/components/schemas/__stringMax64PatternAAZAZ09Z'
          description: Name of a readiness check.
        ResourceSet:
          allOf:
          - $ref: '#/components/schemas/__stringMax64PatternAAZAZ09Z'
          description: Name of the resource set to be checked.
        Tags:
          $ref: '#/components/schemas/Tags'
    GetReadinessCheckStatusRequest:
      type: object
      properties:
        MaxResults:
          allOf:
          - $ref: '#/components/schemas/MaxResults'
          description: The number of objects that you want to return with this call.
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
        ReadinessCheckName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: Name of a readiness check.
      required:
      - ReadinessCheckName
    GetReadinessCheckStatusResponse:
      type: object
      properties:
        Messages:
          allOf:
          - $ref: '#/components/schemas/__listOfMessage'
          description: Top level messages for readiness check status
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
        Readiness:
          allOf:
          - $ref: '#/components/schemas/Readiness'
          description: The readiness at rule level.
        Resources:
          allOf:
          - $ref: '#/components/schemas/__listOfResourceResult'
          description: Summary of the readiness of resources.
    GetRecoveryGroupReadinessSummaryRequest:
      type: object
      properties:
        MaxResults:
          allOf:
          - $ref: '#/components/schemas/MaxResults'
          description: The number of objects that you want to return with this call.
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
        RecoveryGroupName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The name of a recovery group.
      required:
      - RecoveryGroupName
    GetRecoveryGroupReadinessSummaryResponse:
      type: object
      properties:
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
        Readiness:
          allOf:
          - $ref: '#/components/schemas/Readiness'
          description: The readiness status at a recovery group level.
        ReadinessChecks:
          allOf:
          - $ref: '#/components/schemas/__listOfReadinessCheckSummary'
          description: Summaries of the readiness checks for the recovery group.
    GetRecoveryGroupRequest:
      type: object
      properties:
        RecoveryGroupName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The name of a recovery group.
      required:
      - RecoveryGroupName
    GetRecoveryGroupResponse:
      type: object
      properties:
        Cells:
          allOf:
          - $ref: '#/components/schemas/__listOf__string'
          description: A list of a cell's Amazon Resource Names (ARNs).
        RecoveryGroupArn:
          allOf:
          - $ref: '#/components/schemas/__stringMax256'
          description: The Amazon Resource Name (ARN) for the recovery group.
        RecoveryGroupName:
          allOf:
          - $ref: '#/components/schemas/__stringMax64PatternAAZAZ09Z'
          description: The name of the recovery group.
        Tags:
          allOf:
          - $ref: '#/components/schemas/Tags'
          description: The tags associated with the recovery group.
    GetResourceSetRequest:
      type: object
      properties:
        ResourceSetName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: Name of a resource set.
      required:
      - ResourceSetName
    GetResourceSetResponse:
      type: object
      properties:
        ResourceSetArn:
          allOf:
          - $ref: '#/components/schemas/__stringMax256'
          description: The Amazon Resource Name (ARN) for the resource set.
        ResourceSetName:
          allOf:
          - $ref: '#/components/schemas/__stringMax64PatternAAZAZ09Z'
          description: The name of the resource set.
        ResourceSetType:
          allOf:
          - $ref: '#/components/schemas/__stringPatternAWSAZaZ09AZaZ09'
          description: 'The resource type of the resources in the resource set. Enter
            one of the following values for resource type:


            AWS::ApiGateway::Stage, AWS::ApiGatewayV2::Stage, AWS::AutoScaling::AutoScalingGroup,
            AWS::CloudWatch::Alarm, AWS::EC2::CustomerGateway, AWS::DynamoDB::Table,
            AWS::EC2::Volume, AWS::ElasticLoadBalancing::LoadBalancer, AWS::ElasticLoadBalancingV2::LoadBalancer,
            AWS::Lambda::Function, AWS::MSK::Cluster, AWS::RDS::DBCluster, AWS::Route53::HealthCheck,
            AWS::SQS::Queue, AWS::SNS::Topic, AWS::SNS::Subscription, AWS::EC2::VPC,
            AWS::EC2::VPNConnection, AWS::EC2::VPNGateway, AWS::Route53RecoveryReadiness::DNSTargetResource'
        Resources:
          allOf:
          - $ref: '#/components/schemas/__listOfResource'
          description: A list of resource objects.
        Tags:
          $ref: '#/components/schemas/Tags'
    InternalServerException:
      type: object
      properties:
        Message:
          $ref: '#/components/schemas/__string'
      description: An unexpected error occurred.
    LastAuditTimestamp:
      type: string
      format: date-time
      description: <p>The time that a recovery group was last assessed for recommendations,
        in UTC ISO-8601 format.</p>
    ListCellsRequest:
      type: object
      properties:
        MaxResults:
          allOf:
          - $ref: '#/components/schemas/MaxResults'
          description: The number of objects that you want to return with this call.
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
    ListCellsResponse:
      type: object
      properties:
        Cells:
          allOf:
          - $ref: '#/components/schemas/__listOfCellOutput'
          description: A list of cells.
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
    ListCrossAccountAuthorizationsRequest:
      type: object
      properties:
        MaxResults:
          allOf:
          - $ref: '#/components/schemas/MaxResults'
          description: The number of objects that you want to return with this call.
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
    ListCrossAccountAuthorizationsResponse:
      type: object
      properties:
        CrossAccountAuthorizations:
          allOf:
          - $ref: '#/components/schemas/__listOfCrossAccountAuthorization'
          description: A list of cross-account authorizations.
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
    ListReadinessChecksRequest:
      type: object
      properties:
        MaxResults:
          allOf:
          - $ref: '#/components/schemas/MaxResults'
          description: The number of objects that you want to return with this call.
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
    ListReadinessChecksResponse:
      type: object
      properties:
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
        ReadinessChecks:
          allOf:
          - $ref: '#/components/schemas/__listOfReadinessCheckOutput'
          description: A list of readiness checks associated with the account.
    ListRecoveryGroupsRequest:
      type: object
      properties:
        MaxResults:
          allOf:
          - $ref: '#/components/schemas/MaxResults'
          description: The number of objects that you want to return with this call.
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
    ListRecoveryGroupsResponse:
      type: object
      properties:
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
        RecoveryGroups:
          allOf:
          - $ref: '#/components/schemas/__listOfRecoveryGroupOutput'
          description: A list of recovery groups.
    ListResourceSetsRequest:
      type: object
      properties:
        MaxResults:
          allOf:
          - $ref: '#/components/schemas/MaxResults'
          description: The number of objects that you want to return with this call.
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
    ListResourceSetsResponse:
      type: object
      properties:
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
        ResourceSets:
          allOf:
          - $ref: '#/components/schemas/__listOfResourceSetOutput'
          description: A list of resource sets associated with the account.
    ListRulesOutput:
      type: object
      properties:
        ResourceType:
          allOf:
          - $ref: '#/components/schemas/__stringMax64'
          description: The resource type that the readiness rule applies to.
        RuleDescription:
          allOf:
          - $ref: '#/components/schemas/__stringMax256'
          description: The description of a readiness rule.
        RuleId:
          allOf:
          - $ref: '#/components/schemas/__stringMax64'
          description: The ID for the readiness rule.
      required:
      - ResourceType
      - RuleDescription
      - RuleId
      description: Readiness rule information, including the resource type, rule ID,
        and rule description.
    ListRulesRequest:
      type: object
      properties:
        MaxResults:
          allOf:
          - $ref: '#/components/schemas/MaxResults'
          description: The number of objects that you want to return with this call.
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
        ResourceType:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The resource type that a readiness rule applies to.
    ListRulesResponse:
      type: object
      properties:
        NextToken:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The token that identifies which batch of results you want to
            see.
        Rules:
          allOf:
          - $ref: '#/components/schemas/__listOfListRulesOutput'
          description: A list of readiness rules for a specific resource type.
    ListTagsForResourcesRequest:
      type: object
      properties:
        ResourceArn:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The Amazon Resource Name (ARN) for a resource.
      required:
      - ResourceArn
    ListTagsForResourcesResponse:
      type: object
      properties:
        Tags:
          allOf:
          - $ref: '#/components/schemas/Tags'
          description: ''
    MaxResults:
      type: integer
      minimum: 1
      maximum: 1000
    Message:
      type: object
      properties:
        MessageText:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The text of a readiness check message.
      description: Information relating to readiness check status.
    NLBResource:
      type: object
      properties:
        Arn:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The Network Load Balancer resource Amazon Resource Name (ARN).
      description: The Network Load Balancer resource that a DNS target resource points
        to.
    R53ResourceRecord:
      type: object
      properties:
        DomainName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The DNS target domain name.
        RecordSetId:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The Route 53 Resource Record Set ID.
      description: The Route 53 resource that a DNS target resource record points
        to.
    Readiness:
      type: string
      enum:
      - READY
      - NOT_READY
      - UNKNOWN
      - NOT_AUTHORIZED
      description: <p>The readiness status.</p>
    ReadinessCheckOutput:
      type: object
      properties:
        ReadinessCheckArn:
          allOf:
          - $ref: '#/components/schemas/__stringMax256'
          description: The Amazon Resource Name (ARN) associated with a readiness
            check.
        ReadinessCheckName:
          allOf:
          - $ref: '#/components/schemas/__stringMax64PatternAAZAZ09Z'
          description: Name of a readiness check.
        ResourceSet:
          allOf:
          - $ref: '#/components/schemas/__stringMax64PatternAAZAZ09Z'
          description: Name of the resource set to be checked.
        Tags:
          $ref: '#/components/schemas/Tags'
      required:
      - ReadinessCheckArn
      - ResourceSet
      description: A readiness check.
    ReadinessCheckSummary:
      type: object
      properties:
        Readiness:
          allOf:
          - $ref: '#/components/schemas/Readiness'
          description: The readiness status of this readiness check.
        ReadinessCheckName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The name of a readiness check.
      description: Summary of all readiness check statuses in a recovery group, paginated
        in GetRecoveryGroupReadinessSummary and GetCellReadinessSummary.
    ReadinessCheckTimestamp:
      type: string
      format: date-time
      description: <p>The time (UTC) that the cell was last checked for readiness,
        in ISO-8601 format.</p>
    Recommendation:
      type: object
      properties:
        RecommendationText:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: Text of the recommendations that are provided to make an application
            more recovery resilient.
      required:
      - RecommendationText
      description: Recommendations that are provided to make an application more recovery
        resilient.
    RecoveryGroupOutput:
      type: object
      properties:
        Cells:
          allOf:
          - $ref: '#/components/schemas/__listOf__string'
          description: A list of a cell's Amazon Resource Names (ARNs).
        RecoveryGroupArn:
          allOf:
          - $ref: '#/components/schemas/__stringMax256'
          description: The Amazon Resource Name (ARN) for the recovery group.
        RecoveryGroupName:
          allOf:
          - $ref: '#/components/schemas/__stringMax64PatternAAZAZ09Z'
          description: The name of the recovery group.
        Tags:
          allOf:
          - $ref: '#/components/schemas/Tags'
          description: The tags associated with the recovery group.
      required:
      - Cells
      - RecoveryGroupArn
      - RecoveryGroupName
      description: A representation of the application, typically containing multiple
        cells.
    Resource:
      type: object
      properties:
        ComponentId:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The component identifier of the resource, generated when DNS
            target resource is used.
        DnsTargetResource:
          allOf:
          - $ref: '#/components/schemas/DNSTargetResource'
          description: The DNS target resource.
        ReadinessScopes:
          allOf:
          - $ref: '#/components/schemas/__listOf__string'
          description: A list of recovery group Amazon Resource Names (ARNs) and cell
            ARNs that this resource is contained within.
        ResourceArn:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The Amazon Resource Name (ARN) of the Amazon Web Services resource.
      description: The resource element of a resource set.
    ResourceNotFoundException:
      type: object
      properties:
        Message:
          $ref: '#/components/schemas/__string'
      description: The requested resource does not exist.
    ResourceResult:
      type: object
      properties:
        ComponentId:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The component id of the resource.
        LastCheckedTimestamp:
          allOf:
          - $ref: '#/components/schemas/ReadinessCheckTimestamp'
          description: The time (UTC) that the resource was last checked for readiness,
            in ISO-8601 format.
        Readiness:
          allOf:
          - $ref: '#/components/schemas/Readiness'
          description: The readiness of a resource.
        ResourceArn:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The Amazon Resource Name (ARN) of the resource.
      required:
      - LastCheckedTimestamp
      - Readiness
      description: The result of a successful Resource request, with status for an
        individual resource.
    ResourceSetOutput:
      type: object
      properties:
        ResourceSetArn:
          allOf:
          - $ref: '#/components/schemas/__stringMax256'
          description: The Amazon Resource Name (ARN) for the resource set.
        ResourceSetName:
          allOf:
          - $ref: '#/components/schemas/__stringMax64PatternAAZAZ09Z'
          description: The name of the resource set.
        ResourceSetType:
          allOf:
          - $ref: '#/components/schemas/__stringPatternAWSAZaZ09AZaZ09'
          description: 'The resource type of the resources in the resource set. Enter
            one of the following values for resource type:


            AWS::ApiGateway::Stage, AWS::ApiGatewayV2::Stage, AWS::AutoScaling::AutoScalingGroup,
            AWS::CloudWatch::Alarm, AWS::EC2::CustomerGateway, AWS::DynamoDB::Table,
            AWS::EC2::Volume, AWS::ElasticLoadBalancing::LoadBalancer, AWS::ElasticLoadBalancingV2::LoadBalancer,
            AWS::Lambda::Function, AWS::MSK::Cluster, AWS::RDS::DBCluster, AWS::Route53::HealthCheck,
            AWS::SQS::Queue, AWS::SNS::Topic, AWS::SNS::Subscription, AWS::EC2::VPC,
            AWS::EC2::VPNConnection, AWS::EC2::VPNGateway, AWS::Route53RecoveryReadiness::DNSTargetResource'
        Resources:
          allOf:
          - $ref: '#/components/schemas/__listOfResource'
          description: A list of resource objects.
        Tags:
          $ref: '#/components/schemas/Tags'
      required:
      - ResourceSetArn
      - ResourceSetName
      - ResourceSetType
      - Resources
      description: A collection of resources of the same type.
    RuleResult:
      type: object
      properties:
        LastCheckedTimestamp:
          allOf:
          - $ref: '#/components/schemas/ReadinessCheckTimestamp'
          description: The time the resource was last checked for readiness, in ISO-8601
            format, UTC.
        Messages:
          allOf:
          - $ref: '#/components/schemas/__listOfMessage'
          description: Details about the resource's readiness.
        Readiness:
          allOf:
          - $ref: '#/components/schemas/Readiness'
          description: The readiness at rule level.
        RuleId:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The identifier of the rule.
      required:
      - LastCheckedTimestamp
      - Messages
      - Readiness
      - RuleId
      description: The result of a successful Rule request, with status for an individual
        rule.
    TagResourceRequest:
      type: object
      properties:
        ResourceArn:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The Amazon Resource Name (ARN) for a resource.
        Tags:
          allOf:
          - $ref: '#/components/schemas/Tags'
          description: ''
      required:
      - ResourceArn
      - Tags
    TagResourceResponse:
      type: object
      properties: {}
    Tags:
      type: object
      additionalProperties:
        $ref: '#/components/schemas/__string'
      description: <p>A collection of tags associated with a resource.</p>
    TargetResource:
      type: object
      properties:
        NLBResource:
          allOf:
          - $ref: '#/components/schemas/NLBResource'
          description: The Network Load Balancer Resource.
        R53Resource:
          allOf:
          - $ref: '#/components/schemas/R53ResourceRecord'
          description: The Route 53 resource.
      description: The target resource that the Route 53 record points to.
    ThrottlingException:
      type: object
      properties:
        Message:
          $ref: '#/components/schemas/__string'
      description: Request was denied due to request throttling.
    UntagResourceRequest:
      type: object
      properties:
        ResourceArn:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The Amazon Resource Name (ARN) for a resource.
        TagKeys:
          allOf:
          - $ref: '#/components/schemas/__listOf__string'
          description: The keys for tags you add to resources.
      required:
      - ResourceArn
      - TagKeys
    UpdateCellRequest:
      type: object
      properties:
        CellName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The name of the cell.
        Cells:
          allOf:
          - $ref: '#/components/schemas/__listOf__string'
          description: A list of cell Amazon Resource Names (ARNs), which completely
            replaces the previous list.
      required:
      - CellName
      - Cells
    UpdateCellResponse:
      type: object
      properties:
        CellArn:
          allOf:
          - $ref: '#/components/schemas/__stringMax256'
          description: The Amazon Resource Name (ARN) for the cell.
        CellName:
          allOf:
          - $ref: '#/components/schemas/__stringMax64PatternAAZAZ09Z'
          description: The name of the cell.
        Cells:
          allOf:
          - $ref: '#/components/schemas/__listOf__string'
          description: A list of cell ARNs.
        ParentReadinessScopes:
          allOf:
          - $ref: '#/components/schemas/__listOf__string'
          description: The readiness scope for the cell, which can be a cell Amazon
            Resource Name (ARN) or a recovery group ARN. This is a list but currently
            can have only one element.
        Tags:
          allOf:
          - $ref: '#/components/schemas/Tags'
          description: Tags on the resources.
    UpdateReadinessCheckRequest:
      type: object
      properties:
        ReadinessCheckName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: Name of a readiness check.
        ResourceSetName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The name of the resource set to be checked.
      required:
      - ReadinessCheckName
      - ResourceSetName
      description: Name of a readiness check to describe.
    UpdateReadinessCheckResponse:
      type: object
      properties:
        ReadinessCheckArn:
          allOf:
          - $ref: '#/components/schemas/__stringMax256'
          description: The Amazon Resource Name (ARN) associated with a readiness
            check.
        ReadinessCheckName:
          allOf:
          - $ref: '#/components/schemas/__stringMax64PatternAAZAZ09Z'
          description: Name of a readiness check.
        ResourceSet:
          allOf:
          - $ref: '#/components/schemas/__stringMax64PatternAAZAZ09Z'
          description: Name of the resource set to be checked.
        Tags:
          $ref: '#/components/schemas/Tags'
    UpdateRecoveryGroupRequest:
      type: object
      properties:
        Cells:
          allOf:
          - $ref: '#/components/schemas/__listOf__string'
          description: A list of cell Amazon Resource Names (ARNs). This list completely
            replaces the previous list.
        RecoveryGroupName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: The name of a recovery group.
      required:
      - Cells
      - RecoveryGroupName
      description: Name of a recovery group.
    UpdateRecoveryGroupResponse:
      type: object
      properties:
        Cells:
          allOf:
          - $ref: '#/components/schemas/__listOf__string'
          description: A list of a cell's Amazon Resource Names (ARNs).
        RecoveryGroupArn:
          allOf:
          - $ref: '#/components/schemas/__stringMax256'
          description: The Amazon Resource Name (ARN) for the recovery group.
        RecoveryGroupName:
          allOf:
          - $ref: '#/components/schemas/__stringMax64PatternAAZAZ09Z'
          description: The name of the recovery group.
        Tags:
          allOf:
          - $ref: '#/components/schemas/Tags'
          description: The tags associated with the recovery group.
    UpdateResourceSetRequest:
      type: object
      properties:
        ResourceSetName:
          allOf:
          - $ref: '#/components/schemas/__string'
          description: Name of a resource set.
        ResourceSetType:
          allOf:
          - $ref: '#/components/schemas/__stringPatternAWSAZaZ09AZaZ09'
          description: 'The resource type of the resources in the resource set. Enter
            one of the following values for resource type:


            AWS::ApiGateway::Stage, AWS::ApiGatewayV2::Stage, AWS::AutoScaling::AutoScalingGroup,
            AWS::CloudWatch::Alarm, AWS::EC2::CustomerGateway, AWS::DynamoDB::Table,
            AWS::EC2::Volume, AWS::ElasticLoadBalancing::LoadBalancer, AWS::ElasticLoadBalancingV2::LoadBalancer,
            AWS::Lambda::Function, AWS::MSK::Cluster, AWS::RDS::DBCluster, AWS::Route53::HealthCheck,
            AWS::SQS::Queue, AWS::SNS::Topic, AWS::SNS::Subscription, AWS::EC2::VPC,
            AWS::EC2::VPNConnection, AWS::EC2::VPNGateway, AWS::Route53RecoveryReadiness::DNSTargetResource'
        Resources:
          allOf:
          - $ref: '#/components/schemas/__listOfResource'
          description: A list of resource objects.
      required:
      - ResourceSetName
      - ResourceSetType
      - Resources
      description: Name of a resource set.
    UpdateResourceSetResponse:
      type: object
      properties:
        ResourceSetArn:
          allOf:
          - $ref: '#/components/schemas/__stringMax256'
          description: The Amazon Resource Name (ARN) for the resource set.
        ResourceSetName:
          allOf:
          - $ref: '#/components/schemas/__stringMax64PatternAAZAZ09Z'
          description: The name of the resource set.
        ResourceSetType:
          allOf:
          - $ref: '#/components/schemas/__stringPatternAWSAZaZ09AZaZ09'
          description: 'The resource type of the resources in the resource set. Enter
            one of the following values for resource type:


            AWS::ApiGateway::Stage, AWS::ApiGatewayV2::Stage, AWS::AutoScaling::AutoScalingGroup,
            AWS::CloudWatch::Alarm, AWS::EC2::CustomerGateway, AWS::DynamoDB::Table,
            AWS::EC2::Volume, AWS::ElasticLoadBalancing::LoadBalancer, AWS::ElasticLoadBalancingV2::LoadBalancer,
            AWS::Lambda::Function, AWS::MSK::Cluster, AWS::RDS::DBCluster, AWS::Route53::HealthCheck,
            AWS::SQS::Queue, AWS::SNS::Topic, AWS::SNS::Subscription, AWS::EC2::VPC,
            AWS::EC2::VPNConnection, AWS::EC2::VPNGateway, AWS::Route53RecoveryReadiness::DNSTargetResource'
        Resources:
          allOf:
          - $ref: '#/components/schemas/__listOfResource'
          description: A list of resource objects.
        Tags:
          $ref: '#/components/schemas/Tags'
    ValidationException:
      type: object
      properties:
        Message:
          $ref: '#/components/schemas/__string'
      description: The input fails to satisfy the constraints specified by an AWS
        service.
    __listOfCellOutput:
      type: array
      items:
        $ref: '#/components/schemas/CellOutput'
    __listOfCrossAccountAuthorization:
      type: array
      items:
        $ref: '#/components/schemas/CrossAccountAuthorization'
    __listOfListRulesOutput:
      type: array
      items:
        $ref: '#/components/schemas/ListRulesOutput'
    __listOfMessage:
      type: array
      items:
        $ref: '#/components/schemas/Message'
    __listOfReadinessCheckOutput:
      type: array
      items:
        $ref: '#/components/schemas/ReadinessCheckOutput'
    __listOfReadinessCheckSummary:
      type: array
      items:
        $ref: '#/components/schemas/ReadinessCheckSummary'
    __listOfRecommendation:
      type: array
      items:
        $ref: '#/components/schemas/Recommendation'
    __listOfRecoveryGroupOutput:
      type: array
      items:
        $ref: '#/components/schemas/RecoveryGroupOutput'
    __listOfResource:
      type: array
      items:
        $ref: '#/components/schemas/Resource'
    __listOfResourceResult:
      type: array
      items:
        $ref: '#/components/schemas/ResourceResult'
    __listOfResourceSetOutput:
      type: array
      items:
        $ref: '#/components/schemas/ResourceSetOutput'
    __listOfRuleResult:
      type: array
      items:
        $ref: '#/components/schemas/RuleResult'
    __listOf__string:
      type: array
      items:
        $ref: '#/components/schemas/__string'
    __string:
      type: string
    __stringMax256:
      type: string
      minLength: 0
      maxLength: 256
    __stringMax64:
      type: string
      minLength: 0
      maxLength: 64
    __stringMax64PatternAAZAZ09Z:
      type: string
      pattern: ^\A[a-zA-Z0-9_]+\z$
      minLength: 0
      maxLength: 64
    __stringPatternAWSAZaZ09AZaZ09:
      type: string
      pattern: ^AWS::[A-Za-z0-9]+::[A-Za-z0-9]+$
